spring:
  datasource:
    url: jdbc:postgresql://db:5432/postgres
    username: nodir
    password: root123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
